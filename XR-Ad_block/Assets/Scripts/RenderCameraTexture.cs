using UnityEngine;

public class RenderCameraTexture : MonoBehaviour
{
    private GetCameraTexture receivedTexture;
    
    // Update is called once per frame
    void Update()
    {
        if (receivedTexture.currentTexture != null)
        {
            
        }
    }
}
